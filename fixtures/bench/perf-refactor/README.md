# matchkit

Two matching helpers used by the catalogue importer. Both are called with the
full catalogue, which has grown by two orders of magnitude since these were
written. Run the suite with `bun test`.
