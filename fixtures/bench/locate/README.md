# orderkit

Internal helpers for pricing, order totals and the nightly invoice report.

All monetary amounts are integer cents inside the domain; formatting to a
human-readable string happens only in the report layer.
